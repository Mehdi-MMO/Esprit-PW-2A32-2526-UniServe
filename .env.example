# Copier vers `.env` et renseigner les secrets. Chargé au démarrage (Database::ensureEnvLoaded).

# --- MySQL ---
DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=uniserve
DB_USER=root
DB_PASSWORD=

# --- SMTP (mot de passe oublié, OTP) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=tls
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_FROM_NAME=UniServe

# --- Alertes / risque connexion & Gemini (alignement config locale / extensions) ---
LOGIN_ALERT_EMAIL_ENABLED=1
LOGIN_RISK_AI_ENABLED=1
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
CALENDAR_BRIEF_AI_ENABLED=1
CALENDAR_BRIEF_GEMINI_MODEL=gemini-2.0-flash

LOGIN_RISK_MEDIUM_THRESHOLD=35
LOGIN_RISK_HIGH_THRESHOLD=70
LOGIN_RISK_ADMIN_ALERT_EMAIL=

# --- Utilisés par le code à la racine (à garder si tu branches ces fonctionnalités) ---
# Domaines email autorisés (virgules). Vide = défaut interne du Controller.
INSTITUTIONAL_EMAIL_DOMAINS=gmail.com

# Si `DB_PASSWORD` absent : secours PDO (ex. Docker).
MYSQL_ROOT_PASSWORD=

# Widget Assistant IA (`/chatbot/ask`).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
